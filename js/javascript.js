$(document).ready(function(){
//  transitionSpeed of the images interchanging
var transitionSpeed = 5000;
// finding our images with the children method
var itemList = $('ul').children('li');
// finding our pagination links with the children method
var pagination = $('.pagination-carousel').children('a');
// setting itemList to the length property which is allowing us to search through each of our images.
var lengthOfImages = itemList.length;
// set the current postion where we want to start searching
var current = 0;

});
